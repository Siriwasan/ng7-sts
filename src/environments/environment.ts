// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyBD69gcSZUFHN_PahfNvNaCFOXuyR4MrPc',
    authDomain: 'ng7-sts.firebaseapp.com',
    databaseURL: 'https://ng7-sts.firebaseio.com',
    projectId: 'ng7-sts',
    storageBucket: 'ng7-sts.appspot.com',
    messagingSenderId: '395598980728',
    appId: '1:395598980728:web:33ac09cfea1548db'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
